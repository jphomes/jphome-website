# Same-origin API (works on Vercel Services + local via Vite proxy)
VITE_API_URL=/api

# Local-only alternative (PC browser only; will fail on phone):
# VITE_API_URL=http://localhost:5000/api

VITE_WHATSAPP_NUMBER=919893911656
VITE_PHONE_NUMBER=9098887128
VITE_PHONE_DISPLAY=+91 90988 87128
VITE_DISTRICT_NAME=Raipur
