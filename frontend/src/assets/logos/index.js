import logoHorizontal from "./logo-horizontal.png";
import logoMark from "./logo-mark.png";
import logoSquare from "./logo-square.png";
import logoWordmark from "./logo-wordmark.png";

export const logos = {
  /** Full brand lockup — logo + name + tagline (navbar, footer) */
  horizontal: logoHorizontal,
  /** Icon / emblem only (compact headers) */
  mark: logoMark,
  /** Square lockup (admin, share cards) */
  square: logoSquare,
  /** Name + tagline without emblem */
  wordmark: logoWordmark,
};

export default logos;
